mixin breadcrumb(list)
    nav.box-breadcrumb
        .container 
            ul.box-breadcrumb__list
                each item in list 
                    li.box-breadcrumb__item 
                        a(href="#" class = "box-breadcrumb__link") #{item.title}
                        <i class="fa-solid fa-angle-right"></i>